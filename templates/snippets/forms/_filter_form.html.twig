{{form_start(form)}}
    <div class="form-row">
        <div class="form-genres">
            {{form_row(form.genres)}}
        </div>
        <div class="form-watched">
            {{form_row(form.vu)}}
        </div>
        <div class="ok-button">
            {{form_row(form.submit)}}
        </div>
    </div>
{{form_end(form)}}